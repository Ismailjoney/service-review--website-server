Jwt create:
1. download npm i jsonwebtoken
2.const jwt = require(`json` web token`)--require token
3 go to terminal and write node then pren enter
4 write this--> require(`crypto`).randomBytes(64).toString(`hex`) then press enter and create a json web token 
5 set .env file and use this token







vercel deploy code 
{
    "version": 2,
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}